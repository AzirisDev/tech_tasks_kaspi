# Copy to terraform.tfvars and adjust. Do NOT commit real secrets.
region  = "us-east-1"
project = "devops-test"
azs     = ["us-east-1a", "us-east-1b"]

# Provide the DB password via environment instead of this file if possible:
#   export TF_VAR_db_password='...'
# db_password = "change_me_in_prod"
